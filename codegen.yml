schema: "schema.graphql"
documents:
  - "**/*.graphql"
overwrite: true
generates:
  types.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
      - typescript-compatibility
      - fragment-matcher
